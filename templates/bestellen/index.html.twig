{% extends 'userBase.html.twig' %}
{% block title %}Payment | Restaurant Berke{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="alert alert-primary text-center" role="alert">
                    <strong>Uw bestelling tot op in de details</strong>
                </div>
            </div>
        </div>
        <div class="row gy-3">
            <div class="col-md-12">
                <table class="table table-hover">
                    <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Afbeelding</th>
                        <th scope="col">Naam</th>
                        <th scope="col">Prijs</th>
                        <th scope="col">Aantal</th>
                        <th scope="col">Totale prijs</th>
                    </tr>
                    </thead>
                    <tbody>
{#                    {% if order.items %}#}
{#                        {% set number= 1 %}#}
{#                        {% set total=0 %}#}
{#                        {% set price=0 %}#}
{#                        {% set counter= 0 %}#}
{#                        {% for item in order.items %}#}
{#                            <tr>#}
{#                                <th>{{ number }}</th>#}
{#                                <th><img src="{{ asset('img/menu/'~item.product.picture) }}" alt="{{ item.product.picture }}" width="100" height="50px"></th>#}
{#                                <th>{{ item.product.name }}</th>#}
{#                                <th>&euro; {{ item.product.price }}</th>#}
{#                                <th>{{amount_array[counter]}}</th>#}
{#                                <th>&euro; {{ (item.product.price * amount_array[counter])|number_format(2, '.', ',')}}</th>#}
{#                            </tr>#}
{#                            {%  set price = price + (item.product.price * amount_array[counter])|number_format(2, '.', ',')%}#}
{#                            {%  set counter= counter+1 %}#}
{#                            {% set total = total + price %}#}
{#                            {% set number= number+1 %}#}
{#                        {% endfor %}#}
{#                    {% endif %}#}
                    <tr>
                        <th>Totale prijs:</th>
                        <th></th>
                        <th></th>
                        <th></th>
                        <th></th>
                        <th>&euro; {{ finalPrice|number_format(2, '.', ',') }}</th>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="symfony_review_form">
                    {{ form_start(form) }}
                    <div class="form-group">
                        <div class="alert alert-warning px-5" role="alert">
                            GEBRUIK KORTINGSCODE {{ actie }} VOOR 20% KORTING!!!!!
                        </div>
                    </div>
                    <div class="form-group">
                        {{ form_row(form.couponCode) }}
                    </div>
                    <div class="form-group">
                        <input class="btn btn-info text-dark border-dark" type="submit" value="KORTING!">
{#                        {{ form_row(form.save) }}#}
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

