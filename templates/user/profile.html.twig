{% extends 'userBase.html.twig' %}
{% block title %}Profile | Restaurant Berke{% endblock %}
{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <table class="table">
                    <thead>
                    <tr>
                        <th scope="col">Naam</th>
                        <th scope="col">Email</th>
                    </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>{{ user.name }}</td>
                            <td>{{ user.email }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <div class="row">
            <div class="col-md-6">
                <a href="{{ path('user_edit_profile') }}" class="btn btn-warning"><i class="fa-solid fa-pen-to-square"></i>Change profile</a>
            </div>
            <div class="col-md-6">
                <a href="{{ path('user_edit_password') }}" class="btn btn-warning"><i class="fa-solid fa-user-lock"></i>Change password</a>
            </div>
        </div>
    </div>
{% endblock %}